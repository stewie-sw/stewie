{
  "name": "@stewie/design-system",
  "version": "0.1.0",
  "private": true,
  "description": "STEWIE cockpit design system seed: tokens, fonts, icon set, and the core React components. Built from the deployed cockpit's real brand (lunar black + graphite ramp + drum-red accent + Orbitron). The dist/ bundle is what /design-sync uploads to claude.ai/design and the first brick of the front-end rewrite's component library.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run"
  },
  "peerDependencies": {
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@testing-library/react": "^16.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.23.0",
    "jsdom": "^25.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
