# Live cockpit UI eval (2026-06-06)

Harness: `scripts/ui_eval.py` — launches a real headless Chrome (swiftshader) against a running
`planet_browser.server`, plans a real mission (a **blast berm** + a keep-out), runs Execute+watch, then
screenshots all 8 view panes and asserts each rendered with no page JS errors. Screenshots are written to
`validation/ui/*.png` (gitignored — regenerable; the harness + this doc are the durable artifacts).

    PYTHONNOUSERSITE=1 PYTHONPATH=. <venv>/bin/python -m planet_browser.server --host 127.0.0.1 --port 8797 &
    <venv>/bin/python scripts/ui_eval.py --url http://127.0.0.1:8797 --out validation/ui

## Result: EVAL PASSED — 8/8 panes rendered, no page errors

Live plan (blast berm + keep-out @ 20,0 r8): **cut 29.3 t → fill 29.3 t · 125.5 MJ · 29 recharges ·
haul +0.0% around hazards · 1 keep-out · ✓ authority-validated · as-built 3.1 cm ✗ (>2 cm) · autonomy ✓
29rch/0rpl, SoC 95% · est ±0.02 m pose (1 map fix, 1 observe-more) · map 60% covered (1 survey-first)**.

| Pane | Verified by eye |
|---|---|
| **Plan** | LUNAR BUILD PLANNER sidebar (Blast Berm → 2 mass-balanced orders: cut Borrow pit -12,0 + fill Blast berm 0,0; keep-out "1 obstacle @ 20,0 r8"; Plan/Execute/Export/**Plan IR** buttons); the **Moon globe (LRO WAC) rendered** with craters; the 8-tab bar; the Haworth work-area DEM inset. |
| **Report** | the real **mission-control PDF embedded** (moon-build-*.pdf, 1/3): plan table (Borrow pit → Blast berm), MATERIAL BALANCE (cut 29.2 t = fill 29.2 t, 0 surplus/deficit), TOTALS, + chart thumbnails. |
| **Metrics** | execution top-down (cut=blue / fill=orange footprints + green route + rover marker) + telemetry HUD (phase dig, elapsed, position -12,0, moved 1.01 t, battery 100%). Live replay. |
| **Validation** | a real `validation/` figure served (active-perception submodularity plot + greedy uncertainty heatmap) via the `/figures`+`/figure` routes, with the figure dropdown. |
| **Perception** | the Godot PLAN→RENDER pane (honest empty state when no render is run; live SLAM map flagged as the open map-channel work). |
| **API** | FastAPI Swagger `/docs` embedded. |
| **Server** | live `/healthz` + `/metrics` JSON — **`by_route` keyed on route TEMPLATES** (`/dem/{name}`, `/figure/{key:path}`, `/{path:path}`, unmatched:2), confirming the N8 memory-DoS hardening works live. |
| **Config** | the `/config` runtime overlay + the docs-site embed. |

## Honest eval notes (what the eval confirms is REAL)

- The whole authored-berm → plan → report → execute flow works end-to-end in a real browser: the berm
  decomposes to mass-balanced borrow-cut + berm-fill (29.2 t = 29.2 t), routes around the keep-out, and the
  PDF + JSON + Plan IR are all produced.
- The **as-built acceptance correctly FAILS** ("3.1 cm ✗ (>2 cm)") — a uniform berm-fill on real terrain is
  not level to ±2 cm, exactly the honest check the I11 work added.
- The **map-channel survey gate fired** ("1 survey-first") and the **pose fix corrected** (±0.02 m) — the
  P0 honesty fixes are visible in the live numbers.
- No fabricated visuals: the globe is NASA LRO WAC tiles, the figure is a real `validation/` output, the
  metrics are the planner's own forecast (labeled a replay, not live telemetry).

## Gaps surfaced by the eval (none blocking)

- The Perception pane is a render-on-demand empty state until a Godot render is run (render-gated, expected).
- "Execute + watch" is a deterministic forecast replay, not live rover telemetry (the streaming I/O seam is
  P13; correctly labeled).
- The eval is a manual/local harness (needs chrome + playwright), not a CI gate — like the Godot/COLMAP eval
  scripts.
